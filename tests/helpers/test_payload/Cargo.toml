[package]
name = "test_payload"
version = "0.1.0"
edition = "2021"

[dependencies]
winapi = { version = "0.3", features = ["minwindef"] }
dll-syringe-payload-utils = "0.1.3"

[lib]
crate-type = ["dylib"]
